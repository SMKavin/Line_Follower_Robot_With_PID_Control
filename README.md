# Line_Follower_Robot_With_PID_Control
This project implements an autonomous line follower robot that uses IR sensors to detect and follow a path. A microcontroller reads sensor inputs and adjusts motor speeds through an L298N motor driver. A PID control algorithm is used enabling smoother corrections and more stable line following
